# LearnFlow Backend Setup

## 1. Install dependencies

cd backend
pip install -r requirements.txt

## 2. Run the server
cd backend
uvicorn app.main:app --reload

## 3. Run the frontend

cd project
npm run dev
